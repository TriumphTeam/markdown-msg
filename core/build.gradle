group 'me.mattstudios'
version parent.version

dependencies {

    api project(":triumph-msg-commonmark")

    testImplementation "org.junit.jupiter:junit-jupiter-api:5.7.0"
    testImplementation "org.junit.jupiter:junit-jupiter-engine:5.7.0"
}

test {
    useJUnitPlatform()
}
